<%= form_for(@book_list) do |f| %>
  <% if @book_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book_list.errors.count, "error") %> prohibited this book_list from being saved:</h2>

      <ul>
      <% @book_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
      
      <%= f.text_field :category , :maxlength=>250, :size => 61, :placeholder => 'Shelf Name' %></p><td>
      
      <%= f.hidden_field :user_id , :value=>current_user.id %>
  <div class="actions">  
    <%= f.submit %>
  </div>
<% end %>
